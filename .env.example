# Copia este archivo a ".env" (sin ".example") y pon tus valores reales.
# En Render, estas mismas variables se configuran desde el panel de "Environment", no en este archivo.
DATABASE_URL=postgresql://usuario:password@host.neon.tech/basedatos?sslmode=require

# Google OAuth ("Continuar con Google"), desde Google Cloud Console > APIs y servicios > Credenciales
GOOGLE_CLIENT_ID=xxxxxxxxxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxx

# Correos (separados por coma) que pueden gestionar la lista blanca de "Continuar con
# Google" desde admin/usuarios_autorizados.php. El registro público está cerrado.
SUPERADMIN_EMAILS=tu-correo@gmail.com,otro-superadmin@gmail.com

# Correos transaccionales (avisos de autorización/cupo y "olvidé mi contraseña") vía
# Resend (https://resend.com, plan gratuito 100/día). Sin esto, la app funciona igual
# pero no envía correos. MAIL_FROM debe ser un remitente verificado en Resend.
RESEND_API_KEY=re_xxxxxxxxxxxx
MAIL_FROM="MJ Control Systems <avisos@tudominio.com>"

# Monitoreo: webhook (Discord o Slack) que recibe un aviso con cada error fatal en
# producción. Opcional pero muy recomendado.
ERROR_WEBHOOK_URL=https://discord.com/api/webhooks/xxxx/yyyy
